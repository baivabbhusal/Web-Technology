const string1=prompt("Enter first string:");
const string2=prompt("Enter second string:");
if(string1===string2){
    document.write(`${string1.toUpperCase()}`)
}
else{
    document.write("Two string are not equal.")
}

